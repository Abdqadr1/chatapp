server:
  port: 8080
management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  application:
    name: chatroom
  data:
    mongodb:
      auto-index-creation: true
      authentication-database: admin
      username: root
      password: root
      database: chatapp
      port: 27017
      host: localhost
      uri: mongodb://localhost:27017/qadr

